import React from 'react'
import RefreshButton from '.'

const Template = () => <RefreshButton />

export const Default = Template.bind({})

export default {
  title: 'UI/RefreshButton',
  component: RefreshButton,
}
