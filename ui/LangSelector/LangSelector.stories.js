import React from 'react'
import LangSelector from './LangSelector'

const Template = () => <LangSelector />

export const Default = Template.bind({})

export default {
  title: 'UI/LangSelector',
  component: LangSelector,
}
